import 'package:flutter/material.dart';

class CarbonlessMapView extends StatelessWidget {
  const CarbonlessMapView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('map'),
      ),
    );
  }
}
